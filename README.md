# gogrep
a grep tool written in go that uses nfa based regex engine to search for stuff from a file. This is a weekend project, not something that can do actusal stuff

## Todo 
- [] create a nfa based regex engine (tall task)
